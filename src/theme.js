import { createTheme } from "@mui/material";

const theme = createTheme({
  Colors: {
    primary: {
      main: "#fff",
    },
    secondary: {
      main: "#000",
    },
  },

  Fonts: {
    primary: {
      main: "Satoshi",
    },

    secondary: {
      main: "Integral CF",
    },
  },
});

export default theme;
